"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.breakThrough = exports.fallThrough = void 0;
function fallThrough({ value, failBox, isErrorCond }, fn1, fn2, fn3, fn4, fn5, fn6, fn7, fn8) {
    let [totalCount, successCount, failCount] = [0, 0, 0];
    for (const fn of [fn1, fn2, fn3, fn4, fn5, fn6, fn7, fn8].filter((it) => it)) {
        ++totalCount;
        const result = fn(value);
        if (isErrorCond(result)) {
            failCount++;
            failBox.push(result);
            continue;
        }
        successCount++;
    }
    return {
        failReasons: failBox,
        failCount,
        successCount,
        totalCount,
    };
}
exports.fallThrough = fallThrough;
function breakThrough({ value, isErrorCond }, fn1, fn2, fn3, fn4, fn5, fn6, fn7, fn8) {
    let [totalCount, failIndex] = [0, 0];
    const fns = [fn1, fn2, fn3, fn4, fn5, fn6, fn7, fn8].filter((it) => it);
    totalCount = fns.length;
    for (const fn of fns) {
        failIndex++;
        const result = fn(value);
        if (isErrorCond(result)) {
            return {
                failReason: result,
                totalCount,
                failIndex,
            };
        }
    }
    return {
        failReason: null,
        totalCount,
        failIndex: null,
    };
}
exports.breakThrough = breakThrough;
//# sourceMappingURL=inspect.js.map